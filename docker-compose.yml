version: '3.8'

services:
  app:
    build: .
    container_name: frame-pro-crm
    ports:
      - "80:80"
    restart: always
    # Se você precisar de variáveis de ambiente específicas no build, adicione aqui:
    # environment:
    #   - VITE_SUPABASE_URL=...